{
  "type": "object",
  "title": "Upload Parameters",
  "required": [
    "mode"
  ],
  "properties": {
    "mode": {
      "enum": [
        "rewrite",
        "create new"
      ],
      "type": "string",
      "title": "Resolve names conflict strategy",
      "default": "always create new"
    }
  }
}
